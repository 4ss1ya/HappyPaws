<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Разрешения -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HappyPaws"
        tools:targetApi="31">

        <!-- Регистрация питомца -->
<!--        <activity-->
<!--            android:name=".RegisterPetActivity" android:exported="false" />-->

        <!-- Остальные активности -->
        <activity android:name=".RegisterAnimalActivity" android:exported="false" />
        <activity android:name=".RegisterAnimal" android:exported="false" />
        <activity android:name=".PetRulesActivity" android:exported="false" />

        <activity android:name=".MedicalBookActivity" android:exported="false" />
        <activity android:name=".PetRules" android:exported="false" />
        <activity android:name=".Vaccination" android:exported="false" />
        <activity android:name=".MedicalBook" android:exported="false" />

        <!-- App ID -->
        <meta-data
            android:name="com.google.android.gms.auth.api.credentials.APP_ID"
            android:value="@string/app_id" />

        <!-- Google Maps API Key -->
        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="AIzaSyBTXuoccvYCVbkIwVgFUyFMe-gPBufUL3I" />

        <!-- Основные активности -->
        <activity android:name=".LoginActivity" android:exported="false" />
        <activity android:name=".RegisterActivity" android:exported="false" />
        <activity android:name=".GoogleMapActivity" android:exported="false" />
        <activity android:name=".CalendarActivity" android:exported="false" />

        <!-- Главное меню (стартовый экран) -->
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
